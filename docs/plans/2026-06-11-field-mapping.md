# 6 月新表字段映射表（待补充）

> 飞书新表：`tbl9WJyxl9bbtYjb`（wiki 节点 `LRROwulJciI7JYkIT55cQtdpnze`）
> DB 表：`competition_submissions`
> 关联 id：旧表 `tbl12tkH7lOR9rrq`（通过"关联参赛项目.record_ids[0]"）
> 当前前台口径（2026-06-23）：AI 大赛、场景大全和字段管理页展示「推广复用价值系数」`reuseValue`；`reuseValueLevel` / `reuse_value_level` 仅作为历史快照兼容字段保留，不再作为前台展示字段。

**使用方式**：在「新表字段名」列填飞书多维表格的实际字段名（**带原始空格/标点**），我据此改 `FIELD_NAME_MAP` + 前端消费。

---

## 一、卡片正面字段

### 1. 头部

| 5 月字段（前端展示） | 新表字段名（待填） | DB 字段 | 备注 |
|---|---|---|---|
| 标题 | `一句话简述场景` ✓ 已映射 | `title` | |
| 团队 tag | `提报团队` ✓ 已映射 | `team` | |
| 提报人姓名 | `提报人` ✓ 已映射 | `submitter` | |

### 2. 一、复用价值 Hero 条 + 最终价值计分（核心指标）

| 5 月字段（前端展示） | 新表字段名（待填） | DB 字段 | 备注 |
|---|---|---|---|
| 前台展示 | `推广复用价值系数` ✓ 已映射 | `reuse_value` | AI 大赛、场景大全、字段管理页展示此字段 |
| 历史等级字段 | `推广复用价值等级` ✓ 已映射 | `reuse_value_level` | 保留入库兼容，不再作为前台展示字段 |
| x 系数 | regex `/x(\d+)/` from `reuse_value` | 计算 | |
| 预估月省工时 | `月均提效节省工时 × x` | 计算 | |
| **最终价值计分**（新需求，放在 Hero 条下方） | **`最终价值计分`** ✓ 已识别 | 需新增 DB 列 `final_value_score` | **新表字段 = 飞书 "最终价值计分"** |

### 3. 二、量化数据

#### 3.1 三个核心

| 5 月字段（前端展示） | 新表字段名（待填） | DB 字段 | 备注 |
|---|---|---|---|
| 月省工时 | `月均提效节省工时` ✓ 已映射 | `monthly_saved_hours` | |
| 提效比例 | 月均提效节省工时（新表 50 字段里没找到对应"提效比例"，可能由"最终价值计分"取代） | `efficiency_rate` | **需要你定** |
| Token 费/月 | `月均Token费用` ✓ 已映射 | `ai_cost` | |

#### 3.2 2×2 网格

| 5 月字段（前端展示） | 新表字段名（待填） | DB 字段 | 备注 |
|---|---|---|---|
| 原人均每月投入工时 | `原单次执行耗时` ✓ 已映射 | `old_hours_per_task` | 字段名变了（"耗时"而非"工时"） |
| 原月均投入人数 | `原单次执行耗时`  ✓ 已映射 | `before_people_count` | |
| 现人均每月投入工时 | `新单次执行耗时` ✓ 已映射 | `new_duration` | |
| 现月均投入人数 | `新执行人数` ✓ 已映射 | `after_people_count` | |
这些字段，全变了。
改成：
原执行人数、原执行频率、原执行次数、原单次执行耗时，新执行人数、新执行频率、新执行次数、新单次执行耗时。其中：执行频率、执行次数需要做个计算逻辑，结合转换成最终我需要的字段：执行频次，例如：每月1次。
这些字段也要取到数据库：月均降本费用（不含人力成本）、降本费用说明、

#### 3.3 BeforeAfter（操作次数 / 单次时长）

| 5 月字段（前端展示） | 新表字段名（待填） | DB 字段 | 备注 |
|---|---|---|---|
| 原/新操作次数 | `原执行次数` / `新执行次数` ✓ 已映射 | `old_operation_count` / `new_operation_count` | |
| 原/新单次时长 | 同 3.2（已映射到 old_hours_per_task / new_duration） | 同上 | |

### 4. 三、评审得分

| 5 月字段（前端展示） | 新表字段名（待填） | DB 字段 | 备注 |
|---|---|---|---|
| 总分（×2 后） | （不在新表，由 reviews 表聚合） | `totalScore` 计算 | |
| N 人评 | （不在新表，由 reviews 表聚合） | `reviewCount` 计算 | |

**需求 #1**：评审未开始（totalScore=null）不展示卡片。当前筛选：`.eq('status', '评审中')` 已限定，但评审中方案可能 totalScore=null。需要前端在 `totalScore==null` 时跳过渲染。

---

## 二、方案详情 Modal 字段

| 5 月字段 | 新表字段名（待填） | DB 字段 | 备注 |
|---|---|---|---|
| 标题 | `一句话简述场景` | `title` | |
| 团队 tag | `提报团队` | `team` | |
| Track tag | **？？？**（新表没"赛道"字段） | `track` | 这个字段删掉，改成新增字段：核心价值 |
| SceneCategory tag | `场景分类` ✓ 已映射 | `scene_category` | |
| 提报人 | `提报人` | `submitter` | |
| 小组成员 | `组队成员` ✓ 已映射 | `team_members` | |
| 方案确认人 | `工时与降本真实性确认人` ✓ 已映射 | `verifier` | |
| Token 费 | `月均Token费用` | `ai_cost` | |
| 复用价值 | `推广复用价值系数` | `reuse_value` | 前台详情展示系数字段；`reuse_value_level` 仅保留历史兼容 |
| AI 工具 | `AI工具` ✓ 已映射 | `ai_tools` | |
| 核心痛点 | `原核心痛点` ✓ 已映射 | `pain_points` | |
| 原业务场景&流程 | `原业务场景及流程` ✓ 已映射 | `before_process` | |
| 改造后流程 | `新业务流程` ✓ 已映射 | `after_process` | |
| 实现方式 | `实现过程简述` ✓ 已映射 | `implementation` | |
| 其他价值 | `核心价值` ✓ 已映射 | `extra_value` | |
| Demo 链接 |  | `demo_link` | 删掉 |
| 飞书记录 | 这个改成飞书记录对应链接 | 需新增 DB 列或复用 demo_link |  |
| 关联参赛项目 | `关联参赛项目` | 需新增 DB 列 `legacy_submission_id`（已用 record_id 关联） | |
| 价值排名 | **`价值排名`** ✓ 已识别 | 需新增 DB 列 | 删除，不需要这个字段 |
| 最终价值计分 | 同上 | 需新增 DB 列 | **同上** |
| 实现效果链接 | **`实现效果`** ✓ 已识别 | 需新增 DB 列 `implementation_link` 或复用 | **新表字段 = 飞书 "实现效果"** 没错，就按照这个来 |

---
